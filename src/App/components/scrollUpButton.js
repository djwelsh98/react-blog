import ScrollUpButton from 'react-scroll-up-button';

function ScrollUp() {
    return (
        <div>
            <ScrollUpButton
                AnimationDuration={500}
            />
        </div>
    );
}

export default ScrollUp